
const page = () => {
    // eslint-disable-next-line @typescript-eslint/no-unused-vars
    const serviceUsername = "Hasan"
    return (
        <div>
            {serviceUsername}
        </div>
    );
};

export default page;